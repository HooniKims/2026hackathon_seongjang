# 사례집 자동 생성 — 데이터 스키마 & 루브릭 명세

> 작성일: 2026-08-10
> 근거 자료
> - `제1호 교사 개발자 해커톤 사례집_수정.pdf` (92p) — 실제 지면 구조 역추출
> - `260804_장학사님과의 통화.pdf` (18p) — 강윤지 장학사 운영 철학
> - `교사개발자 성장형 강사진 회의록.pdf` (6p, 2026-08-09~10) — 강사진 합의사항
> - `DESIGN.md` — sendev.kr 디자인 시스템

---

## 0. 이 문서가 푸는 문제

강윤지 장학사와 강사진이 반복해서 지적한 문제는 **하나**로 수렴합니다.

> "이왕이면 1번 할 때 하고, 그걸 공유하거나 활용" — 통화
> "기존 제출 공간은 결과물이 많이 올라와도 하나씩 열어봐야 하며, 유형·대상·목적별 검색과 필터링이 어려웠다" — 회의록
> "이미 제출할 때부터 카테고리화를 하면, 자동으로 통계가 나온다" — 통화

즉 **기록을 두 번 하지 않게 만들고(단일 입력), 제출 시점에 분류를 강제해(구조화) 통계가 저절로 나오게(자동 집계)** 하는 것.

이 문서는 그 "단일 입력"의 필드를 확정한다.

```
연수생 짧은 입력 ──▶ AI 1차 초안 ──▶ 연수생 수정·확정 ──┬──▶ 사례집 페이지(웹)
   (구조화된 폼)      (Solar)         (반드시 수정 필요)   ├──▶ A4 PDF 보고서
                                                          ├──▶ 현장 루브릭 평가
                                                          └──▶ 통계 대시보드
```

---

## 1. 사례집 지면 구조 (역추출 결과)

제1호 사례집은 **1팀 = 4페이지(2스프레드)** 고정 포맷이다.

```
PART 1. 프롤로그, 변화의 서막        — 현장 리포트, 멘토·기업 인터뷰
PART 2. 현장의 목소리, 인간 중심 기술로 답하다
   Chapter 1. 행정의 늪에서 교육의 뜰로     (p.24~68)
   Chapter 2. 교실을 깨우는 기술            (p.64~110)
   Chapter 3. 연결과 보호의 울타리          (p.112~158)
PART 3. (에필로그)
```

> **Chapter 1·2·3이 곧 "웹앱 유형 카테고리"다.** 새로 만들지 말고 이걸 그대로 쓴다.

### 팀별 4페이지 구성

| 페이지 | 섹션 | 내용 |
|---|---|---|
| **1** | 헤더 | Chapter명 · 상태배지(●◎○) · `</title>` 서비스명 · 해시태그 3개 |
| | **0.1%의 빈틈, 현장의 발견** | 문제 발견 불릿 2~4개 |
| | 팀원 | 학교 + 이름 (+ 각자 선택한 윤리 약속) |
| | **솔루션 제안: 빛의 설계도** | 요구조건(FR/NFR/핵심목표) + 타당성 논증(왜/가치/기대효과) |
| | 활용 플랫폼 및 기술 스택 | 프론트엔드 / 백엔드 / LLM·API / 바이브 코딩 툴 |
| **2** | **뜨거운 몰입, 치열한 개발기** | 6단계의 여정 (각 단계 서술형) |
| **3** | Main Visual | 대표 스크린샷 + 프로그램 링크 / GitHub / 데모 ID·PW (QR) |
| | 핵심기능 1 · 2 | 각각 스크린샷 + 설명 |
| | 개인 서사 | "펜 대신 키보드를 잡게 된 결정적 순간" |
| | 기대 효과 | "기술들이 교실에 어떤 변화를 가져오길 바랐는지" |
| **4** | **교사 개발자 윤리** | 일곱가지 약속 중 팀원별 선택 + 서술 · 구현영상 QR · 코드 QR |

### 상태 배지 (그대로 계승)

```
● 배포중 (Live)      실제 학교에서 운영 중
◎ 베타테스트 (Beta)  일부 사용자 대상 시험 운영
○ 프로토타입 (Demo)  동작하는 시연 수준
```

회의록의 *"완성도, 공개 동의, 배포 상태를 구분해야 한다"* 를 이 배지가 담당한다.

### 교사 개발자의 일곱가지 약속

```
1. 책임과 출처 존중        5. 역할 경계 인식
2. 학생 성장 최우선        6. 공공성
3. 개인정보·데이터 보호    7. 투명성 및 설명 가능성
4. 안전한 실험과 검증
```

장학사가 말한 *"일반 개발자들 명세서에서는 볼 수 없는 요소"* 가 바로 이것. **루브릭의 뿌리이기도 하다.**

---

## 2. 데이터 스키마

원칙 세 가지.

1. **트랙 공통 스키마 1개.** 회의록: *"입문형·성장형·도전형이 각각 다른 플랫폼과 데이터 구조를 만들기보다 공통 대문과 분류 체계를 사용해야 한다."*
2. **필수/선택만 트랙별로 다르다.** 성장형은 미완성이 정상이므로 6단계·핵심기능이 선택.
3. **분류 필드는 제출 시점에 강제.** 그래야 통계가 자동으로 나온다.

### A. 식별·분류 — 대시보드의 근거가 되는 필드

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `title` | string | ✅ | 서비스명 (예: `AI SEN for Google Sheets`) |
| `tagline` | string(60) | ✅ | 한 줄 소개 — 회의록 "도구 이름·한 줄 소개" |
| `track` | enum | ✅ | `intro`(입문형) / `growth`(성장형) / `challenge`(도전형) |
| `category` | enum | ✅ | `admin`(행정의 늪에서 교육의 뜰로) / `classroom`(교실을 깨우는 기술) / `care`(연결과 보호의 울타리) |
| `tags` | string[] | ✅ | 3개 권장. 예: `#멀티LLM #구글시트연동 #AI함수구현` |
| `status` | enum | ✅ | `live` ● / `beta` ◎ / `demo` ○ |
| `targetUsers` | enum[] | ✅ | `teacher` / `student` / `parent` / `admin` |
| `problemDomain` | enum | ✅ | 문제ZIP 영역 재사용 — 보건·건강 / 행정·공문 / 수업·평가 / 학부모소통 / 학교행사 / 직접입력 |
| `publishConsent` | boolean | ✅ | 공개 동의 — 회의록 명시 항목 |
| `team` | `{school, name, role}[]` | ✅ | 3인 1팀 기준 |

> 🎯 이 10개 필드만 제출 시점에 받으면 **회의록이 요구한 대시보드 지표가 전부 자동 산출된다**:
> "결과물 개수뿐 아니라 많이 제기된 학교 문제, 공감이 높은 아이디어, 재사용·확산 현황"

### B. 문제 발견 — `0.1%의 빈틈, 현장의 발견`

| 필드 | 타입 | 필수 | 지면 대응 |
|---|---|---|---|
| `gapDiscovery` | string[] | ✅ | 불릿 2~4개 |
| `whyThisProblem` | text | ✅ | "왜 이 문제인가?" |
| `whatValue` | text | ✅ | "어떤 가치가 있는가?" |
| `expectedImpact` | text | ✅ | "해결 시 기대되는 교육적·사회적 효과?" |

### C. 솔루션 설계 — `솔루션 제안: 빛의 설계도`

| 필드 | 타입 | 필수 |
|---|---|---|
| `functionalReq` | text | ✅ | 기능적 요구(FR) |
| `nonFunctionalReq` | text | ✅ | 비기능적 요구(NFR) |
| `coreGoal` | text | ✅ | 프로젝트의 핵심 목표 |

### D. 기술 스택 & 링크

| 필드 | 타입 | 필수 |
|---|---|---|
| `stack.frontend` | string | ✅ |
| `stack.backend` | string | ✅ |
| `stack.llmApi` | string | — |
| `stack.vibeCodingTool` | string | ✅ | 예: Codex, Cursor, GitHub Copilot, Antigravity |
| `links.programUrl` | url | — | 배포 링크 |
| `links.githubUrl` | url | — | **입력 시 저장소 메인페이지 자동 미리보기** |
| `links.demoId` / `demoPw` | string | — | 심사용 데모 계정 |

### E. 개발 여정 — `뜨거운 몰입, 치열한 개발기`

6단계 고정. **성장형은 3~4단계까지만 채워도 유효**(회의록: 미완성 인정).

| 필드 | 단계명 |
|---|---|
| `journey.step1` | 초기 설계 |
| `journey.step2` | 첫 UI 구축 |
| `journey.step3` | 기능 단위 구현 |
| `journey.step4` | 통신·API 연동 등 추가 기능 |
| `journey.step5` | 오류 수정 및 최종 검증 |
| `journey.step6` | 완성된 시스템 또는 앱 화면 |

### F. 결과물 시각자료

| 필드 | 타입 | 필수 |
|---|---|---|
| `mainVisual` | image | ✅ | 대표 화면 |
| `feature1` | `{title, desc, image}` | ✅ | 핵심기능 1 |
| `feature2` | `{title, desc, image}` | — | 핵심기능 2 |

### G. 교사의 목소리 — AI가 대신 쓰면 안 되는 부분

| 필드 | 타입 | 필수 | 지면 질문 |
|---|---|---|---|
| `turningPoint` | text | ✅ | "펜 대신 키보드를 잡게 된 결정적인 순간" |
| `hopedChange` | text | ✅ | "기술들이 교실에 어떤 변화를 가져오길 바라셨는지" |

> ⚠️ 이 두 필드는 **AI 초안 생성 대상에서 제외**하거나, 생성하더라도 "반드시 본인 경험으로 다시 쓰기"를 강제한다. 장학사가 말한 *"선생님 자체가 마인드 자체를 교사에 더 방점을 찍어라"* 가 살아있는 곳이 여기다.

### H. 윤리 — `교사 개발자의 일곱가지 약속`

| 필드 | 타입 | 필수 |
|---|---|---|
| `ethics[]` | `{memberName, promise(enum 7), statement(text)}` | ✅ | 팀원 1인당 최소 1개 |

### I. 성장형 전용 (미완성 허용 보완 필드)

회의록: *"결과물이 미완성일 수 있는 성장형의 특성을 고려하여, 문제·사용자·구현 범위·강점·보완점이 드러나는 간단한 설명 양식을 제공"*

| 필드 | 타입 | 설명 |
|---|---|---|
| `scopeImplemented` | text | 어디까지 구현했는가 |
| `strengths` | text | 강점 |
| `limitations` | text | 보완점 |
| `peerFeedback` | text | 동료 사용자 테스트에서 받은 피드백 |
| `nextIteration` | text | 반영 여부·우선순위 판단 |

---

## 3. AI 초안 생성 규칙

### 입력 → 출력 매핑

연수생은 **짧게** 쓰고 AI가 확장한다. 확장 대상과 비대상을 명확히 가른다.

| 구분 | 필드 | AI 역할 |
|---|---|---|
| 🤖 **AI 확장** | `gapDiscovery` `whyThisProblem` `whatValue` `expectedImpact` `functionalReq` `nonFunctionalReq` `coreGoal` `journey.step1~6` `feature1/2.desc` | 짧은 메모 → 사례집 문체로 확장 |
| ✍️ **본인 작성** | `turningPoint` `hopedChange` `ethics[].statement` | AI 초안 금지 또는 강제 재작성 |
| 🔒 **입력 그대로** | `title` `tagline` `tags` `stack.*` `links.*` `team` | 변형 금지 |

### 저작권 안전장치 (쟁점 6 대응)

사용자 결정: *"AI 초안을 그대로 저장되지 않게 하고, 반드시 수정하도록"*

구현 방식:
1. AI 초안은 `draft` 상태로만 저장. `published` 전환 불가
2. 각 필드별로 **초안 원문 해시**를 보관 → 확정 시 비교
3. 한 글자도 안 고친 필드가 있으면 확정 버튼 비활성 + 해당 필드 하이라이트
4. 확정 화면에 "AI 초안을 검토·수정했습니다" 체크 필수

> 기존 sendev 저작권 팝업("다시 보지 않기" 체크박스 패턴)을 그대로 재사용하면 UI 일관성이 유지된다.

---

## 4. 루브릭 (추천안)

### 설계 근거

| 근거 문장 | 출처 | 반영 항목 |
|---|---|---|
| "교사의 관점 80, 기술자의 관점 20" | 회의록 5 | 1·2번 가중 |
| "진짜 필요한, 공감 가는 이런 부분들을 선발" | 통화 | ① 현장성 |
| "기술 습득 자체보다 학교 현장의 문제 해결" | 회의록 전체요약 | ② 교육적 가치 |
| "결과물의 가치는 완성도만이 아니라 문제 발견, 시도, 피드백, 개선 과정에도 있다" | 회의록 공통의견 | ③ 실행력 (완성도 아님) |
| "배포로 끝내지 않고 사용 종료, 데이터 정리·삭제 등 사후 관리" | 회의록 10 | ④ 안전과 책임 |
| "재사용·확산 현황을 파악할 수 있어야 교육청 사업의 근거" | 회의록 9 | ⑤ 확산 가능성 |
| "일관성 있게 성장형에도 적용되고 도전형에도 똑같이 적용" | 통화 | 트랙 공통 5항목 |

### 평가 항목 — 5항목 × 5점(0.5점 단위) = 25점

| # | 항목 | 묻는 질문 | 낮음(1) ↔ 높음(5) |
|---|---|---|---|
| **①** | **현장성**<br>진짜 문제인가 | 교실·학교에서 실제로 겪는 문제이고, 다른 교사도 공감할 만한가? | 개인 취향의 아이디어 ↔ 많은 교사가 겪는 구체적 현장 문제 |
| **②** | **교육적 가치**<br>학생의 배움으로 이어지는가 | 업무 효율을 넘어 학생의 배움과 성장에 닿아 있는가? | 기술 시연에 그침 ↔ 교육적 목적이 설계에 녹아 있음 |
| **③** | **실행력**<br>실제로 동작하는가 | 문제를 동작하는 형태로 옮겼는가? (완성도 ≠ 점수) | 아이디어만 존재 ↔ 핵심 기능이 실제로 작동 |
| **④** | **안전과 책임**<br>지속 운영을 고려했는가 | 개인정보·윤리·사후관리를 고려했는가? | 고려 없음 ↔ 일곱가지 약속이 구체적으로 반영됨 |
| **⑤** | **확산 가능성**<br>남이 가져다 쓸 수 있는가 | 다른 학교·교사가 재사용할 수 있게 기록·공개되었는가? | 본인만 사용 가능 ↔ 문서·링크·라이선스까지 갖춤 |

### 서술형 1문항 (필수)

> **"이 팀에게 가장 도움이 될 한 가지 조언을 적어주세요." (100자 이내)**

근거: 통화에서 *"선택과 그다음에 서술형 평가"*, 회의록 8번 *"동료 피드백에서 잘된 점과 개선점"*.
점수보다 이 텍스트가 연수생에게 더 가치 있다.

### 트랙별 가중치 (선택 적용)

| 항목 | 성장형 | 도전형 |
|---|---|---|
| ① 현장성 | ×1.2 | ×1.0 |
| ② 교육적 가치 | ×1.2 | ×1.0 |
| ③ 실행력 | ×0.8 | ×1.2 |
| ④ 안전과 책임 | ×0.8 | ×1.2 |
| ⑤ 확산 가능성 | ×1.0 | ×1.0 |

> 성장형은 *"백엔드 완성을 일률적으로 요구하지 않는다"*(회의록 결정사항 4)이므로 실행력 비중을 낮추고 문제 발견에 무게를 둔다. **단, 항목 자체는 동일**하므로 장학사가 요구한 "일관성"은 유지된다.

### 평가 방식

- **현장 발표를 들으면서** 즉시 입력 (사용자 확정 사항 — 블라인드·셔플 불필요)
- 별점 **5점 만점, 0.5점 단위** — 기존 sendev 평가 UI와 동일
- 제출 즉시 집계 → *"집계도 필요 없이 바로 그냥 나온다"*(통화)

---

## 5. 출력 — 하나의 디자인, 두 개의 크기

> ⚠️ 확정 사항: **그레이스케일 안 씀.** DESIGN.md를 준수하고 sendev.kr과 통일감을 유지한다.
> 달라지는 것은 **색이 아니라 크기**뿐이다.

| | 웹 화면 (sendev 안) | A4 출력 / PDF 다운로드 |
|---|---|---|
| 팔레트 | DESIGN.md 민트 `#11b981` | **동일** |
| 폰트 | Pretendard | **동일** |
| 아이콘 | lucide | **동일** |
| 카드·라운드 | `rounded-2xl` `shadow-sm` | **동일** |
| 폭 | `max-w-5xl` (대시보드만 `max-w-[1800px]`) | **A4 210×297mm 세로 고정** |
| 헤더 | sendev 공통 헤더 | 보고서 제목 + 우측 끝 `1/10` 페이지 번호 |
| 차트 | `--chart-1~5` 토큰 | **동일 토큰, HTML·CSS로만 구현** (PDF 저해상도 깨짐 방지) |
| 페이지 | 연속 스크롤 | `page-break` 로 페이지 안 넘어가게 조정 |

구현 방식:
```css
@page { size: A4 portrait; margin: 0; }
.a4-page {
  width: 210mm; min-height: 297mm;
  page-break-after: always;
  break-inside: avoid;
}
@media print {
  .no-print { display: none; }
  .a4-page { box-shadow: none; }
}
```
- 화면에서는 A4 시트를 카드처럼 보여주고(`shadow-sm`), 인쇄 시 그림자를 제거한다
- **"PDF 다운로드" 버튼 = `window.print()`** → 사용자가 "PDF로 저장" 선택. 별도 라이브러리 불필요
- 색이 그대로 나가므로 컬러 인쇄·화면 열람 모두 자연스럽다

---

## 6. 윤리 데이터 & 인사이트 도출 ⭐

> PART 3 분석 결과 추가된 절.
> 강윤지 장학사 (2026-08-10 메시지): *"단순히 사례집 내용 분류를 넘어서는 **인사이트 도출**까지?!"*
> *"이 부분이 일반 다른 해커톤과의 차별점으로 우리 교육청이 고민하며 진행하고 있는 차별화 포인트"*

### 작년에 손으로 한 것

제1호 사례집 PART 3은 **이미 인사이트 도출 결과물**이다.

```
33명 교사 개발자
  ├─ 7대 원칙에 점수 부여        → "점수 데이터 분석: 숫자로 증명된 3대 핵심 가치" (27명 응답)
  └─ 서술형 의견 작성            → "서술형 응답 분석: 기술의 가능성보다 교육의 필요성을 먼저 묻다"
                                     → 종합 분석: 합의된 원칙과 현실적 과제
                                     → 교사 개발자 윤리 선언 (제1조~제7조)
                                     → 윤리 자기 점검 체크리스트
```

**이 파이프라인이 곧 올해 자동화 대상이다.** 사례집 페이지를 모으는 것은 수단이고, 목적은 여기서 나오는 집단 인사이트다.

### 추가 스키마 — J. 윤리 설문

| 필드 | 타입 | 설명 |
|---|---|---|
| `ethicsSurvey.scores` | `{principle(enum 7): number}` | 7대 원칙 각각에 중요도 점수 |
| `ethicsSurvey.openEnded` | text | 서술형 — "내가 추가하고 싶은 교사 개발자 윤리는?" |
| `ethicsChecklist` | `{item: string, checked: boolean}[]` | 배포 전 자기 점검 (7대 원칙 대응) |

> 작년 서술형 응답 예시: *"제가 추가하고 싶은 교사 개발자 윤리는 '실패를 허용하는 문화'입니다…"*
> → 이런 응답이 모이면 **8번째 원칙**이 나온다. 그게 인사이트다.

### 대시보드가 뽑아야 할 인사이트 (단순 집계 ✗)

| 층위 | 지표 | 예시 출력 |
|---|---|---|
| 집계 | 결과물 수, 카테고리 분포, 상태 비율 | "성장형 24건, 행정 11 / 수업 9 / 소통보호 4" |
| **패턴** | 많이 제기된 학교 문제 | "`행정·공문` 영역이 전체의 46% — 3년째 1위" |
| **패턴** | 공감이 높은 아이디어 | 좋아요·평가 상위 + 유사 문제 클러스터 |
| **인사이트** | 윤리 점수 상위 3대 가치 | "학생 성장 최우선 · 개인정보 보호 · 안전한 검증" |
| **인사이트** | 서술형 응답 주제 추출 | Solar로 클러스터링 → "실패 허용 문화" 같은 신규 원칙 후보 |
| **인사이트** | 기술 스택 트렌드 | "Codex 사용률 전년 대비 ↑, Firebase 백엔드 정착" |
| **정책 근거** | 재사용·확산 현황 | "Live 상태 8건 중 5건이 타 학교 도입" |

회의록 9번: *"결과물 개수뿐 아니라 많이 제기된 학교 문제, 공감이 높은 아이디어, 재사용·확산 현황을 파악할 수 있어야 **교육청 사업의 근거**가 된다"*

---

## 7. 확정된 범위 (2026-08-10)

| 항목 | 결정 |
|---|---|
| **트랙** | **성장형(`growth`)만 우선 구현.** 입문형·도전형은 스키마만 열어두고 UI는 미구현 |
| `problemDomain` 선택지 | 도전형 소관 → **이번 범위 제외** |
| **평가자** | **강사진 + 동료 연수생 둘 다.** 단 **"강사진 제외" 토글**을 두어 동료 평가만으로도 집계 가능하게 |
| 디자인 | DESIGN.md 준수, sendev 통일감 유지. A4는 **크기만** 맞춤 |
| PDF | 브라우저 인쇄(`window.print()`) 기반 다운로드 |

## 8. 남은 미결

1. **AI 초안 생성 단위** — 필드별 개별 생성 vs 전체 일괄 생성
2. **윤리 설문 시점** — 사례집 작성 시 / 별도 설문 / 발표 후
3. **동료 평가 익명 여부** — 기존 sendev는 닉네임 기반이라 반익명
4. **성장형 필수 필드 최종 확정** — 6단계 여정을 몇 단계까지 필수로 볼 것인가
